pipeline {
    agent { lable 'slave2' }
    stages {
        stage('Example') {
            steps {
                echo "hello-world-war"
            }
        stage {
            stag('Example2') {
            echo "hello-world-war"
            }
        }
    }
}
}
